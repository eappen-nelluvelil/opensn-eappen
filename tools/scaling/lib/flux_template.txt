#!/bin/sh
#flux: --job-name={{base_name}}_{{n_nodes}}
#flux: --nodes={{n_nodes}}
#flux: --queue={{partition}}
#flux: --exclusive
# Full-node allocation is Flux's portable analogue of Slurm --mem=0.
#flux: --time=60m
#flux: --output={{outdir}}/{{base_name}}_{{n_nodes}}.out
#flux: --error={{outdir}}/{{base_name}}_{{n_nodes}}.err
{% if processor == "gpu" and gpu_options -%}
{{gpu_options}}
{% endif %}
cd {{outdir}}
flux run -N {{n_nodes}} -n {{n_nodes * n_tasks}} -c {{n_cores}}{% if processor == "gpu" %} -g 1 -o cpu-affinity=per-task -o gpu-affinity=per-task{% endif %} {{opensn_binary}} -i {{input_script}}
